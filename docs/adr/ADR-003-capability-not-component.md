# ADR-003: Capability is not a component

**Status:** Accepted

A capability describes a semantic user ability such as `select-date` or `compare-items`. A component is one brand-specific implementation of that capability. Core planning must reason about capabilities, not component names.
