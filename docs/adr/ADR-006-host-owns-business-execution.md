# ADR-006: Host owns business execution

**Status:** Accepted

Vira Enterprise GenUI emits actions but does not become the customer's business backend. The host application or adapter executes business-side operations and returns normalized state/data updates.
