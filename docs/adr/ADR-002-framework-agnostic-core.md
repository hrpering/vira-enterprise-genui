# ADR-002: Framework-agnostic core

**Status:** Accepted

Protocol, runtime-core, planner, and composer must not depend on React or browser-specific rendering APIs. Framework integrations are wrappers around the same runtime.
