# GENUI-124 review branch note

`feat/genui-flight-pack-migration-clean` is the authoritative review branch for GENUI-124.

The earlier `feat/genui-flight-pack-migration` branch was used as a scratch/assembly branch. A connector safety gate prevented rewriting that ref after the final squash, so review and subsequent stacking must use the clean branch instead.

Clean head parent: `be9f04549d4996521eeff2cb1e79c5c629a7258b`.
