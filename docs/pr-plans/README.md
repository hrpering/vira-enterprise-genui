# Active PR Plans

This directory contains only work that is currently active or required to close the current release.

Current records:

- `MASTER-25.md` — implemented Enterprise RC gate contract retained as the release interface being closed.
- `MASTER-25R.md` — active post-CLEAN-00 exact-head RC evidence closure.
- `MASTER-25-native-qc-note.md` — retained evidence note for the active release gate.
- `ACTIVE_PHASE.md` — machine/human-readable status pointer.

`CLEAN-00` merged via PR #184 and its records are historical release evidence; completed implementation plans live under `docs/archive/` or their dedicated evidence directories rather than being treated as active execution authority.

Do not pre-create future MASTER phase plans here. MASTER-26 starts only after MASTER-25R merges, Enterprise GenUI RC1 is truthfully declared, and the latest authoritative `main` is reverse engineered.
