# POL-001 — Policy Engine v1

## Status

Reconciled onto authoritative `main` `fe40163c5d4af38b9486e6c2a5b1f063fd55a4a0`.

Branch: `feat/policy-engine-v1`.

## Ownership

Policy Engine is a thin orchestration boundary over the existing canonical `security` package. Security remains authoritative for capability, component and network policy semantics.

## Boundary

`policy-engine -> security` only.

## Invariants

- closed v1 policy-check kinds only;
- nested policy validation delegated to `security` rather than duplicated;
- valid deny remains distinct from invalid policy configuration;
- wrapper accessors/symbol state fail closed;
- arbitrary targets, URLs, policy values and unknown field names are not echoed in sanitized errors;
- no execution, persistence, identity, entitlement or provider-specific policy surface is introduced.

## Acceptance

1. Policy decisions match the existing security-owned evaluators;
2. invalid wrapper/policy/target inputs fail closed;
3. output is bounded, frozen and does not echo sensitive target content;
4. latest-main diff contains only Policy owner files plus its single package-boundary declaration;
5. exact reconciled-head hosted CI and adversarial review pass;
6. local repository CI remains deferred to the final all-phase integration SHA.

## Merge rule

Do not merge independently. All Experience Platform phases will first be assembled into one exact integration tree and the user will run the full local CI gate once before any merge.