# Fixtures

Store small, human-readable, deterministic contract fixtures here.

Fixture files must contain normalized/canonical data only. Do not commit production prompts, secrets, PII, raw customer payloads, or captured authentication/session material.
